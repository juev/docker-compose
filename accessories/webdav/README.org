* webdav

Firstly create ~.env~ file from example and fill values:

#+begin_src shell
  $ cp env.example .env
#+end_src

After that just run containers:

#+begin_src shell
  $ docker-compose up -d
#+end_src
